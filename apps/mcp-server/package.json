{
  "name": "@frontend-ai-context-kit/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "smoke": "tsx src/smoke.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@frontend-ai-context-kit/prompt-rules": "workspace:*",
    "zod": "^3.24.2"
  }
}

